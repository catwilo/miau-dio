# miau-dio runs with zero external dependencies (Python stdlib only).
# Install via ./install.sh -- no pip required.
[project]
name = "miau-dio"
version = "0.2.0"
description = "Modular CLI for synthesizing and managing musical ideas"
requires-python = ">=3.10"
